use crate::{board::square::Square, chess::ChessBoard, piece::PieceColor};

pub fn _move_rook(
    _color: &PieceColor,
    _start_sq: &Square,
    _end_sq: &Square,
    _board: &ChessBoard,
) -> bool {
    true
}
