use crate::{chessboard::square::Square, piece::PieceColor};

pub fn _move_rook(
    _color: &PieceColor,
    _start_sq: &Square,
    _end_sq: &Square,
    _board: &[[Square; 8]; 8],
) -> bool {
    true
}
