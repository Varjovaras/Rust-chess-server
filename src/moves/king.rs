use crate::{
    board::{square::Square, ChessBoard},
    piece::PieceColor,
};

pub fn _move_king(
    _color: &PieceColor,
    _start_sq: &Square,
    _end_sq: &Square,
    _board: &ChessBoard,
) -> bool {
    true
}

mod check {}
